# Source Profile Placeholder

Replace this directory with a browser profile that CronPlus should copy for each
run, or set `runtime.browser.profile_source` to another directory.

Keep real account cookies, tokens, browser history, and extension secrets out of
git.
